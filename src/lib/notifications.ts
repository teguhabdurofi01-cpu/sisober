import "server-only"; export type Email={to:string;subject:string;text:string}; export interface EmailNotificationAdapter{send(v:Email):Promise<{id:string}>} export class MockEmailAdapter implements EmailNotificationAdapter{sent:Email[]=[];async send(v:Email){this.sent.push(v);return{id:`mock-${this.sent.length}`}}} export function emailAdapter():EmailNotificationAdapter{return new MockEmailAdapter()}
