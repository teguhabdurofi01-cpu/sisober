const allowed=new Map([["image/jpeg",["jpg","jpeg"]],["image/png",["png"]],["application/pdf",["pdf"]]]);export function validateEvidenceFile(name:string,mime:string,bytes:Buffer,max=10*1024*1024){const ext=name.split(".").pop()?.toLowerCase()??"";if(!allowed.get(mime)?.includes(ext))throw new Error("Unsupported file type");if(!bytes.length||bytes.length>max)throw new Error("Invalid file size");const magic=bytes.subarray(0,8).toString("hex");if(mime==="application/pdf"&&!bytes.subarray(0,5).equals(Buffer.from("%PDF-")))throw new Error("File content is not PDF");if(mime==="image/png"&&!magic.startsWith("89504e470d0a1a0a"))throw new Error("File content is not PNG");if(mime==="image/jpeg"&&!magic.startsWith("ffd8ff"))throw new Error("File content is not JPEG");}
