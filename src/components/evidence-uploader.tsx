"use client";
import {useRef,useState} from "react";import {useRouter} from "next/navigation";import {FileUp,LoaderCircle} from "lucide-react";
export function EvidenceUploader({submissionId}:{submissionId:string}){const router=useRouter(),formRef=useRef<HTMLFormElement>(null),[busy,setBusy]=useState(false),[error,setError]=useState(""),[progress,setProgress]=useState(0);
async function legacyUpload(formData:FormData){formData.set("submission_id",submissionId);const response=await fetch("/api/evidence",{method:"POST",body:formData}),body=await response.json();if(!response.ok)throw new Error(body.error||"Unggah bukti gagal");}
async function upload(formData:FormData){const file=formData.get("file");if(!(file instanceof File))return;setBusy(true);setError("");setProgress(5);try{const init=await fetch("/api/evidence/init",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({submissionId,evidenceType:formData.get("evidence_type"),filename:file.name,mimeType:file.type,fileSize:file.size})}),prepared=await init.json();if(init.status===409){await legacyUpload(formData);setProgress(100);}else{if(!init.ok)throw new Error(prepared.error||"Tidak dapat menyiapkan unggahan");setProgress(15);await new Promise<void>((resolve,reject)=>{const xhr=new XMLHttpRequest();xhr.open("PUT",prepared.url);Object.entries(prepared.headers as Record<string,string>).forEach(([key,value])=>xhr.setRequestHeader(key,value));xhr.upload.onprogress=(event)=>{if(event.lengthComputable)setProgress(15+Math.round(event.loaded/event.total*75));};xhr.onload=()=>xhr.status>=200&&xhr.status<300?resolve():reject(new Error("R2 menolak unggahan"));xhr.onerror=()=>reject(new Error("Koneksi unggahan terputus"));xhr.send(file);});setProgress(92);const complete=await fetch("/api/evidence/complete",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({evidenceId:prepared.evidenceId})}),done=await complete.json();if(!complete.ok)throw new Error(done.error||"Verifikasi unggahan gagal");setProgress(100);}formRef.current?.reset();router.refresh();}catch(e){setError(e instanceof Error?e.message:"Unggah bukti gagal")}finally{setBusy(false)}}
return <form ref={formRef} action={upload} className="rounded-xl border border-dashed border-[var(--line)] bg-slate-50/70 p-4"><div className="flex items-center gap-2"><FileUp size={18} className="text-[var(--accent)]"/><h3 className="text-sm font-bold">Unggah bukti penyelesaian</h3></div><div className="mt-3 grid gap-3 sm:grid-cols-[1fr_11rem_auto]"><input aria-label="File bukti" className="field" type="file" name="file" accept="image/jpeg,image/png,image/webp,application/pdf,video/mp4,video/quicktime,video/webm" required disabled={busy}/><select aria-label="Jenis bukti" className="field" name="evidence_type" defaultValue="AFTER" disabled={busy}><option value="AFTER">Setelah perbaikan</option><option value="BEFORE">Sebelum perbaikan</option><option value="DOCUMENT">Dokumen</option><option value="OTHER">Lainnya</option></select><button className="btn" disabled={busy}>{busy?<><LoaderCircle className="mr-2 animate-spin" size={16}/>{progress}%</>:"Unggah"}</button></div>{busy?<div className="mt-3 h-2 overflow-hidden rounded-full bg-slate-200"><div className="h-full bg-[var(--accent)] transition-all" style={{width:`${progress}%`}}/></div>:null}<p className="mt-2 text-xs text-[var(--muted)]">JPG, PNG, WebP, PDF, MP4, MOV, atau WebM. Maksimum 250 MB dengan R2.</p>{error?<p role="alert" className="mt-3 rounded bg-red-50 p-2 text-sm text-red-700">{error}</p>:null}</form>}
