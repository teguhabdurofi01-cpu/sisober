import Link from "next/link";import {requestPasswordReset} from "@/app/actions";
export default async function Forgot({searchParams}:{searchParams:Promise<{sent?:string;error?:string}>}){const p=await searchParams;return <main className="grid min-h-screen place-items-center p-5"><section className="panel w-full max-w-sm p-7"><h1 className="text-2xl font-black">Pulihkan akun</h1><p className="mt-2 text-sm text-[var(--muted)]">Masukkan email akun. Jika terdaftar, tautan pemulihan akan dikirim.</p>{p.sent&&<p className="mt-4 rounded bg-emerald-50 p-3 text-sm text-emerald-800">Periksa inbox email atau Mailpit untuk lingkungan lokal.</p>}{p.error&&<p className="mt-4 rounded bg-red-50 p-3 text-sm text-red-700">{p.error}</p>}<form action={requestPasswordReset} className="mt-6 space-y-4"><label className="block text-sm font-bold">Email<input className="field mt-1" type="email" name="email" required/></label><button className="btn w-full">Kirim tautan pemulihan</button></form><Link className="mt-5 block text-center text-sm font-bold text-[var(--accent)]" href="/login">Kembali ke login</Link></section></main>}
