import {completePasswordReset} from "@/app/actions";import {PasswordInput} from "@/components/password-input";
export default async function Reset({searchParams}:{searchParams:Promise<{error?:string}>}){const p=await searchParams;return <main className="grid min-h-screen place-items-center p-5"><section className="panel w-full max-w-sm p-7"><h1 className="text-2xl font-black">Buat kata sandi baru</h1><p className="mt-2 text-sm text-[var(--muted)]">Minimal 10 karakter dengan huruf besar, huruf kecil, dan angka.</p>{p.error&&<p className="mt-4 rounded bg-red-50 p-3 text-sm text-red-700">{p.error}</p>}<form action={completePasswordReset} className="mt-6 space-y-4"><label className="block text-sm font-bold">Kata sandi baru<PasswordInput className="mt-1" name="new_password" minLength={10} required/></label><label className="block text-sm font-bold">Konfirmasi<PasswordInput className="mt-1" name="confirm_password" minLength={10} required/></label><button className="btn w-full">Simpan kata sandi</button></form></section></main>}
