import {redirect} from "next/navigation";import {requireUserOrRedirect as requireUser} from "@/lib/supabase/server";import {createFinding} from "@/app/actions";
export default async function New(){const ctx=await requireUser();if(ctx!.profile.role!=="SUPER_ADMIN")redirect("/findings");const [{data:companies},{data:categories},{data:sections}]=await Promise.all([ctx!.db.from("companies").select("id,code,name").eq("active",true),ctx!.db.from("categories").select("id,name").eq("active",true),ctx!.db.from("sections").select("id,name").eq("active",true)]);return <><h1 className="text-2xl font-black">Buat temuan</h1><form action={createFinding} className="panel mt-6 grid max-w-3xl gap-4 p-5 sm:grid-cols-2">
<label className="text-sm font-bold">Tanggal<input name="finding_date" type="date" required className="field mt-1"/></label><label className="text-sm font-bold">Perusahaan<select name="company_id" required className="field mt-1">{companies?.map(x=><option key={x.id} value={x.id}>{x.code} — {x.name}</option>)}</select></label>
<label className="text-sm font-bold">Kategori<select name="category_id" required className="field mt-1">{categories?.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></label><label className="text-sm font-bold">Section<select name="section_id" required className="field mt-1">{sections?.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></label>
<label className="text-sm font-bold">Nomor unit<input name="unit_number" className="field mt-1" placeholder="Contoh: DT STM HLG 893"/></label><label className="text-sm font-bold">Lokasi<input name="location" className="field mt-1"/></label><label className="text-sm font-bold">Target<input name="target_date" type="date" className="field mt-1"/></label><label className="text-sm font-bold">Bukti<select name="evidence_requirement" className="field mt-1"><option>REQUIRED</option><option>OPTIONAL</option><option>NONE</option></select></label>
<label className="text-sm font-bold sm:col-span-2">Keterangan singkat<textarea name="detailed_description" required className="field mt-1 min-h-24" placeholder="Contoh: tyre no. 12 mengalami aus"/></label><button className="btn sm:col-span-2">Simpan temuan</button></form></>}
