import {describe,it,expect} from "vitest";import * as XLSX from "xlsx";import {mkdtemp} from "node:fs/promises";import {join} from "node:path";import {tmpdir} from "node:os";import {parseSummary} from "../scripts/import-core";
describe("Excel import",()=>{it("parses mixed Indonesian months and flags duplicate rows deterministically",async()=>{const dir=await mkdtemp(join(tmpdir(),"sisober-xlsx-")),file=join(dir,"x.xlsx"),wb=XLSX.utils.book_new(),head=[null,"NO","NO.","DATE","MONTH","YEARS","FINDING NEGATIF CATEGORY","DETAILED DESCRIPTION","COMPANY NAME","SECTION HAULING","KETERANGAN"],row=[null,1,1,15,"JUNE",2026,"APAR","Test","STM","SAPROLITE","Close"];XLSX.utils.book_append_sheet(wb,XLSX.utils.aoa_to_sheet([head,row,row]),"SUMMARY");XLSX.writeFile(wb,file);const a=parseSummary(file),b=parseSummary(file);expect(a.rows[0].date).toBe("2026-06-15");expect(a.report.possibleDuplicates).toEqual([3]);expect(a.rows).toEqual(b.rows)})});
