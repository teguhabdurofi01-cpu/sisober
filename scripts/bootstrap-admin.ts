import {createClient} from "@supabase/supabase-js";const email=process.argv[2];if(!email)throw new Error("Usage: tsx scripts/bootstrap-admin.ts admin@example.com");const db=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.SUPABASE_SERVICE_ROLE_KEY!,{auth:{persistSession:false}});const {data,error}=await db.auth.admin.listUsers();if(error)throw error;const u=data.users.find(x=>x.email?.toLowerCase()===email.toLowerCase());if(!u)throw new Error("Create the Auth user first");const {error:e}=await db.from("profiles").upsert({id:u.id,email:u.email!,full_name:u.user_metadata.full_name??email.split("@")[0],role:"SUPER_ADMIN",active:true});if(e)throw e;console.log(`SUPER_ADMIN ready: ${email}`);
