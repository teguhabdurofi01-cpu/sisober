import {createClient} from "@supabase/supabase-js";
const companies=["BMS","JAM","MIM","MRP","PMS","REAL","STM","TII"];
async function main(){const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!url||!key)throw new Error("Missing Supabase environment");const db=createClient(url,key,{auth:{persistSession:false}});const listed=await db.auth.admin.listUsers({perPage:1000});if(listed.error)throw listed.error;for(const code of companies){const email=`contractor.${code.toLowerCase()}@sisober.local`,password=`${code}-Contractor2026!`;let user=listed.data.users.find(x=>x.email===email);if(!user){const made=await db.auth.admin.createUser({email,password,email_confirm:true,user_metadata:{full_name:`${code} Contractor`}});if(made.error)throw made.error;user=made.data.user}else{const changed=await db.auth.admin.updateUserById(user.id,{password,email_confirm:true});if(changed.error)throw changed.error}const company=await db.from("companies").select("id").eq("code",code).single();if(company.error)throw company.error;const profile=await db.from("profiles").upsert({id:user.id,email,full_name:`${code} Contractor`,role:"CONTRACTOR",active:true});if(profile.error)throw profile.error;const cleared=await db.from("user_company_access").delete().eq("user_id",user.id);if(cleared.error)throw cleared.error;const access=await db.from("user_company_access").insert({user_id:user.id,company_id:company.data.id});if(access.error)throw access.error;console.log(`${code}: ${email}`)}}
main().catch(error=>{console.error(error);process.exitCode=1});
